import { SegmentFilterPipe } from "./segment-filter.pipe";

describe("SegmentFilterPipe", () => {
  it("create an instance", () => {
    const pipe = new SegmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
